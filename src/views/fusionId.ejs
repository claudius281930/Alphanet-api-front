<!DOCTYPE html>
<html lang="en">
<!-- Metadados da página -->
<%- include("./partials/head") %>

    <body>
        <h1>Identificador da Fusão</h1>
        <% if (idFusion) { %>
            <ul>
                <li>ID: <%= idFusion.id %>
                </li>
                <li>Chave estrangeira: <%= idFusion.boxId %>
                </li>
                <li>Data de Modificação: <%= idFusion.dateModify %>
                </li>
                <li>Numero de Fusões: <%= idFusion.numberFusions %>
                </li>
                <li>ID do objeto de relacionamento: <%= idFusion.box_id %>
                </li>

            </ul>
            <% } else { %>
                <p>Box não encontrada.</p>
                <% } %>
                    <!-- Footer da pagina -->
                    <%- include("./partials/footer") %>
                        <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    </body>

</html>