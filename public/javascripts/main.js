//const src = "https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js";

/*const form = document.getElementById("searchForm");
const searchResults = document.getElementById("searchResults");

form.addEventListener("submit", async (event) => {
  event.preventDefault();

  const searchText = document.getElementById("searchText").value;

  try {
    const response = await axios.get(`/api/search?query=${searchText}`);
    const data = response.data;

    // Exiba os resultados da busca na div "searchResults"
    searchResults.innerHTML = JSON.stringify(data);
  } catch (error) {
    console.error(error);
  }
});*/

